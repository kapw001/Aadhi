<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="colorPrimary">@color/dashboard_text</item>-->
        <!--<item name="colorPrimaryDark">@color/dashboard_text</item>-->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="NavigationBarStyle">
        <item name="android:textSize">16sp</item>
        <item name="android:listDivider">@color/navigation_line_color</item>
        <item name="android:background">@color/white</item>
    </style>

    <style name="ActionBarMaterialTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- By default, Material's Toolbar does not have a theme of its own.
        But, we can set those properties. These values, when assigned to v7
        AppCompat's toolbar would override all the settings provided in the
        theme -->
        <item name="colorPrimary">@color/toolbar_background</item>
        <!--<item name="android:background">@color/toolbar_background</item>-->
        <item name="android:background">@color/toolbar_background</item>
        <item name="android:textColorPrimary">@color/white</item>
        <!--<item name="searchIcon">@drawable/ic_search_white_36dp</item>
        <item name="searchHintIcon">@drawable/ic_search_white_36dp</item>-->
        <item name="searchViewStyle">@style/Theme.AppCompat</item>
    </style>

    <style name="EditextTheme" pparent="TextAppearance.AppCompat">
        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/white</item>
        <item name="android:textSize">20sp</item>
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="colorAccent">@color/white</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="colorControlActivated">@color/events</item>
    </style>

    <style name="App_PoojaDetails_Cell">
        <item name="android:layout_width">match_parent</item>
        <item name="android:maxWidth">200dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:paddingLeft">4dp</item>
        <item name="android:paddingRight">4dp</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">12sp</item>
        <item name="android:singleLine">true</item>
        <item name="android:background">@color/navigation_drawer_item_default</item>
    </style>

    <style name="App_PoojaDetails_Cell.Description">
        <item name="android:layout_height">100dp</item>
        <item name="android:singleLine">false</item>
        <item name="android:gravity">start|top</item>
    </style>

    <style name="App_PoojaDetails_Cell.Number">
        <item name="android:inputType">number</item>
        <item name="android:maxLength">10</item>
    </style>

    <style name="App_Common_Menus_Title" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="App_Common_Menus_CONTENT" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="App_Matrimony_Cell">
        <item name="android:layout_width">match_parent</item>
        <item name="android:maxWidth">200dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:paddingLeft">4dp</item>
        <item name="android:paddingRight">4dp</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">10sp</item>
        <item name="android:singleLine">true</item>
        <item name="android:background">#2E2623</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
